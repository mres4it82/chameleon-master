platform :ios, '12.0'

# Use the environment variable to get the correct Flutter SDK path
flutter_application_path = ENV['FLUTTER_ROOT']

# Load the podhelper.rb from the correct Flutter SDK path
load File.join(flutter_application_path, 'packages', 'flutter_tools', 'podhelper.rb')

target 'Runner' do
  use_frameworks!
  use_modular_headers!

  # Add Flutter as a pod
  pod 'Flutter', :path => flutter_application_path

  # Your other dependencies
  pod 'DKPhotoGallery', :modular_headers => true
  pod 'SDWebImage', :modular_headers => true

  target 'RunnerTests' do
    inherit! :search_paths
  end
end
