platform :ios, '12.0'

# In your Podfile
# Inside your Podfile, update the path for podhelper.rb:
flutter_root = File.expand_path('..', __FILE__)
podhelper_path = File.join(flutter_root, '.flutter', 'flutter_tools', 'bin', 'cache', 'artifacts', 'flutter_tools', 'podhelper.rb')

if File.exist?(podhelper_path)
  load podhelper_path
else
  raise "podhelper.rb not found at #{podhelper_path}. Run `flutter pub get` and retry."
end





target 'Runner' do
  use_frameworks!
  use_modular_headers!

  pod 'Flutter', :path => flutter_application_path

  # Your other dependencies
  pod 'DKPhotoGallery', :modular_headers => true
  pod 'SDWebImage', :modular_headers => true

  target 'RunnerTests' do
    inherit! :search_paths
  end
end
